# Replication Guide — Submission Package and Reproduction Instructions

**Thesis**: *An Auditable Multi-Agent LLM Framework for U.S. REIT Investment Decisions:
Decomposable Rationales Beyond a Tuned Structured-ML Baseline* (Lankun Chen, MPhil in Real
Estate Finance, 2026). **Repository state**: commit `COMMIT_HASH_AT_PACKAGING` (the placeholder is substituted with the exact hash in the packaged desktop copy of this guide).
Every number in the thesis reconciles to `CANONICAL_RESULTS.md`, the single ledger of
record; nothing in the text is computed anywhere else.

---

## 0. Quickest start

```bash
git clone https://github.com/programming-thinker/auditable-reit-llm-framework.git
cd auditable-reit-llm-framework
# download the data archives from the v1.0-submission release
gh release download v1.0-submission -p 'data.tar.gz' -p 'outputs.tar.gz' -p 'audit_log_local.tar.gz'
# (or fetch the same files from the Releases page in a browser)
tar xzf data.tar.gz && tar xzf outputs.tar.gz && tar xzf audit_log_local.tar.gz
pip install -r requirements.txt
make reproduce_v6      # golden-snapshot regression: key tables to 6 decimal places
```

`filings.tar.gz` (291 MB compressed) is needed only to rebuild disclosure inputs from
scratch and can be skipped for the baseline reproduction.

## 1. What to submit (three parts)

**Part 1 — Code repository (git).** Version-controlled and complete in itself for code
review: `src/` (structured-ML pipeline), `analysis/` (every table/figure generator),
`llm/` (agents, orchestrator, EDGAR client, post-processing), `paper/` (thesis build
chain: markdown → LaTeX → Overleaf zips), `tests/`, `config/` (universe, prompt lock),
`Makefile`, `requirements.txt`, and the committed part of `audit_log/` — the schema
(`audit_log/SCHEMA.md`), cost ledgers, the masked-identity probe logs, the v3
corrected-feed run (`audit_log/v3_corrected_feed/decisions.jsonl`, `predictions_v3.csv`),
and both human raters' materials (`audit_log/rater2/`, spot-check worksheets).

**Part 2 — Data archives (GitHub Release assets).** These directories are
`.gitignore`d for size and are published as release assets on the same repository
(release `v1.0-submission`); download each and extract into the repository root:

| Directory / file | Size | Needed for |
|---|---|---|
| `data_raw.tar.gz` — the raw layer alone (Yahoo prices, FRED, EDGAR submission indexes; every file listed in `DATA.md`) | 2.8 MB (13 MB extracted) | auditing sources / rebuilding everything from scratch |
| `data/` (raw + interim + processed panels, full bundle) | 0.8 GB | rebuilding features and all baseline results |
| `outputs/` (every result CSV, golden snapshots, figures) | 0.5 GB | direct inspection and the `reproduce_v6` diff |
| `audit_log/decisions.jsonl` + `predictions.csv` (main v2 test run) | ~10 MB | replaying every LLM decision without an API key |
| `.cache/llm_responses/` (diskcache of raw API responses) | optional | byte-level replay of individual calls |
| `filings/` (SEC filing text + raw HTML) | 4.6 GB (291 MB compressed) | *optional*: only to rebuild disclosure inputs from scratch or re-run agents live |

**Part 3 — Documents.** This guide, `CANONICAL_RESULTS.md`, `DATA.md`, and
`SUPPLEMENT.md` — the thesis supplementary materials in full (universe and variable
definitions, agent prompts with SHA pins, reproducibility map, audit schema), ending
with a section-by-section reproduction path. The dissertation text itself is submitted
separately through the examination process and is not distributed here.

## 2. Environment

- Python 3.11+; `make install` (installs `requirements.txt`: pandas, pyarrow,
  scikit-learn, statsmodels, matplotlib, pydantic v2, diskcache, tenacity, structlog).
- LaTeX only if rebuilding the PDF locally: TeX Live 2022+ with XeLaTeX and biber
  (Overleaf handles both automatically).
- No API key is required for any reproduction step below; a DeepSeek key is needed only
  for *fresh* LLM calls (Section 5).

## 3. One-command verification

```bash
make reproduce_v6   # re-runs the V6 baseline (4-fold purged CV + tuning) and diffs
                    # the key tables against golden snapshots to 6 decimal places
make test           # unit tests; LLM tests use recorded responses, no live API
```

`reproduce_v6` passed at packaging time on the submitted data archive.

## 4. Exhibit-by-exhibit map

Each thesis exhibit is generated by one script reading files under `outputs/`; figures 2–8 are drawn by
`analysis/make_figures.py`; Figure 1 is a schematic.

| Exhibit | Generator script | Principal data files (`outputs/…`) |
|---|---|---|
| Table 1 (labels + descriptives) | `analysis/make_tables_tex.py` | `tables/quant_only_*`, processed panel |
| Table 2 (confusion + robustness) | `analysis/make_tables_tex.py` | `tables/quant_only_confusion_matrix_test.csv`, `fundamentals_robustness/tuned_robustness.csv` |
| Table 3 (OLS OOS R² + Fama–MacBeth) | `analysis/extended_ols_fmb.py`, `analysis/fama_macbeth_nw.py` | `tables/ols_metrics_enriched.csv`, `llm_deepseek_test/extended_*`, `llm_deepseek_test/fama_macbeth_nw.csv` |
| Table 4 (main comparison + AUC) | `analysis/build_llm_comparison.py`, `analysis/inference_robustness.py`, `analysis/econ_value_eval.py` | `llm_deepseek_test/headline_comparison.csv`, `llm_deepseek_test/significance_bootstrap.csv`, `fundamentals_robustness/inference_robustness.csv`, `fundamentals_robustness/power_mde.csv`, `fundamentals_robustness/auc_metrics.csv` |
| Table 5 (channel ablations) | `analysis/ablation_channels.py` | `llm_deepseek_test/ablation_channels.csv` |
| Table 6 (systematic decomposition) | `analysis/systematic_vs_idiosyncratic.py` | `fundamentals_robustness/systematic_vs_idiosyncratic.csv`, `fundamentals_robustness/market_adjusted_labels.csv` |
| Table 7 (economic value) | `analysis/econ_value_eval.py` | `fundamentals_robustness/portfolio_value.csv` |
| Table 8 (grounding/judge/raters) | `analysis/factuality_audit.py`, `analysis/rationale_quality_audit.py` | `llm_deepseek_test/factuality_audit.csv`, `rationale_quality_audit_summary.csv`, `spotcheck_agreement.csv` |
| Figures 2–8 | `analysis/make_figures.py` | `figures_v2/*.pdf` (regenerated in one run) |

Figures regenerate in one run of `analysis/make_figures.py`; tables re-derive from the
CSV files listed above.

## 5. The LLM layer: replay vs fresh runs

- **Replay (no key).** Every one of the 575 test decisions is a line in
  `audit_log/decisions.jsonl` carrying prompt SHAs, an input hash, all five agent
  outputs, and final probabilities. `llm/postprocess.py` rebuilds `predictions.csv`
  from the log; `analysis/build_llm_comparison.py` takes it from there. The v3
  corrected-feed rerun and the masked-identity probe replay the same way from their
  own committed logs.
- **Prompt integrity.** Prompts are versioned in `llm/prompts/` and SHA-pinned in
  `config/config.yaml` (lock timestamp 2026-06-26). `make prompt_sha` recomputes the
  hashes; they must match both the config and the per-decision log entries.
- **Fresh runs (key required).** `make llm_dev_run` (1 REIT × 2 months) smoke-tests the
  pipeline; the full test window is one-shot and refuses to run without the lock
  timestamp. Deterministic decoding (temperature 0, pinned model version
  `deepseek-v4-flash`) makes repeated calls cache-identical via
  `.cache/llm_responses/`.

## 6. Integrity anchors

- `CANONICAL_RESULTS.md` — the number ledger every thesis figure traces to.
- `audit_log/SCHEMA.md` — decision-record schema (append-only log).
- `SUPPLEMENT.md` — the thesis supplement: prompt SHA pins, variable definitions, and
  the audit-log schema in context, with its own reproduction path.
- Process records (four rounds of bibliography verification, the pre-submission
  claim-calibration record, the page-by-page format audit) are retained by the author
  and available on request; they concern the thesis text rather than the numbers.
